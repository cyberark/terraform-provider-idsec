---
page_title: "terraform-provider-idsec - idsec_identity_user_attributes_schema"
subcategory: "Identity"
description: The Identity service user attributes schema resource that is used to manage user attributes schema.
---

# idsec_identity_user_attributes_schema (Resource)

The Identity service user attributes schema resource that is used to manage user attributes schema.

## Example Usage

```terraform
resource "idsec_identity_user_attributes_schema" "myuser_attributes_schema" {
  columns = [
    {
      name = "EmployeeNumber_Attr1"
      type = "Text"
    },
    {
      name = "CostCenter_Attr2"
      type = "Text"
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `columns` (Attributes List) List of attribute columns to upsert (see [below for nested schema](#nestedatt--columns))

### Optional

- `total_count` (Number) Total number of attribute schema columns

<a id="nestedatt--columns"></a>
### Nested Schema for `columns`

Required:

- `name` (String) Internal name of the attribute column (e.g., CustomAtt_1)
- `type` (String) Data type of the attribute column (e.g., Text, Int, Bool)

Optional:

- `description` (String) Description of the attribute column
- `title` (String) Display title of the attribute column
- `user_editable` (Boolean) Indicates if the attribute column is editable by the user

